# YesApp Backend - Production Environment
# Copiez ce fichier vers .env.production et remplacez les valeurs

# API Key - Utilisez la même clé que VITE_API_KEY dans le dashboard
# Générez avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=YOUR_GENERATED_API_KEY_HERE

# Node environment
NODE_ENV=production

# Server port
PORT=3000

# Database path
DATABASE_PATH=/app/data/db.sqlite

# Sessions storage path
SESSIONS_PATH=/app/data/sessions
